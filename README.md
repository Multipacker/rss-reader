# RSS Reader

A simple RSS and Atom aggregator and reader. So far you can only see articles
from the feeds, but maybe we will have a way of reading articles here as well.

## TODO

* Save the date something got imported into the database
* Server side search
* Pagination
* Fetch tags/categories that you can filter by
* Dynamic update frequency per feed based on when we get new items
* Organize feeds by user category
* Allow deleting feed entries after a certain amount of time
  * Usefull for news or govenment feeds which might post a lot
* Fetch old items via the Wayback Machine
  * Maybe use other feed aggregators as well?
* Store descriptions if available
  * Depending on length we could show them inline or in a separate window

## Name ideas:

* AggRss (Aggress)
* FeeAgg (Figg)
* AtomAg (SilverFeed)
